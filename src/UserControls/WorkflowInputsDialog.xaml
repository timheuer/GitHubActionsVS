<Window x:Class="GitHubActionsVS.UserControls.WorkflowInputsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workflow Inputs"
        Width="520"
        MinWidth="440"
        SizeToContent="Height"
        MaxHeight="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,8">
            <TextBlock Text="Provide inputs to run this workflow"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,2"/>
            <TextBlock Text="Fields marked with * are required."
                       FontStyle="Italic"
                       Opacity="0.75"/>
        </StackPanel>

        <Separator Grid.Row="1" Margin="0,4,0,8"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
            <Grid x:Name="InputsPanel"
                  Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </ScrollViewer>

        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Text=""
                   Foreground="DarkRed"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    MinWidth="96"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button x:Name="RunButton"
                    Content="Run Workflow"
                    MinWidth="120"
                    IsDefault="True"
                    Click="RunButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
